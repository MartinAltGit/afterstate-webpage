/**
 * Re-export the Afterstate PageLayout from its layout module.
 * Kept so existing imports of `~/components/PageLayout` continue to work.
 */
export {PageLayout, type PageLayoutProps} from '~/components/layout/PageLayout';
