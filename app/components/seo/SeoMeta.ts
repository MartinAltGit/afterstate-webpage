/**
 * SeoMeta helpers — re-export of page title / meta tag builders
 * for route `meta` functions.
 */
export {
  buildPageTitle,
  buildMetaTags,
  clampSeoText,
  type SeoMetaInput,
  type MetaDescriptor,
} from '~/lib/seo/meta';
